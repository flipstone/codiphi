# cost_measure element is required and accepts a string
#
cost_measure "points"

# models are optional, technically
# a model element requires:
#   - an identifier 
#
# Attributes block is optional and 
# may be declared ad hoc
# 
# attr 'cost' will be used to calculate
# list cost if list includes model
#
model :hero {
  cost 100
}

# list requirement
# the list element is required
# a list element requires:
#   - an identifer
#
# list allows one or more (demands/permits) instructions
#
list :force_organization { 
  demands 1 :hero model 
}
